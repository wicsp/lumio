[project]
name = "lumio-bilibili-video-summary"
version = "0.1.0"
description = "Local helpers for extracting Bilibili subtitles for Lumio."
requires-python = ">=3.9"
dependencies = [
  "pycryptodome>=3.20.0",
]

[tool.uv]
package = false
